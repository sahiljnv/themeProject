import {StyleSheet} from 'react-native';

export const myStyle = (fontSize: number, color: string) =>
  StyleSheet.create({
    textStyle: {
      fontSize: fontSize,
      color: color,
    },
  });
